#include <cmath>
#include <iostream>

float f(float x, float a);

int main()
{
	float a = -100;

	float astart = -222813, aend = 0;

	for (; a < 100; a += 0.001) {
		if (std::min (std::min (f (:
	}
}

float f(float x, float a)
{
	return 8 * x - 4 * a;
}

